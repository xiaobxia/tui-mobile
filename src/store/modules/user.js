const user = {
  state: {
    userInfo: {}
  },
  mutations: {
    SET_USER_INFO: (state, info) => {
    }
  },
  actions: {
    appLogin({ commit }, userInfo) {
    },
    getUserInfo({ commit }, noAuth) {
    }
  }
}

export default user
